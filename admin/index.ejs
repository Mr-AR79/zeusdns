<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow, noarchive, nosnippet, noodp, notranslate, noimageindex">
  <meta name="googlebot" content="noindex, nofollow, noarchive">
  <meta name="bingbot" content="noindex, nofollow">
  <meta name="slurp" content="noindex, nofollow">
  <meta name="yandex" content="noindex, nofollow">
  <meta name="baiduspider" content="noindex, nofollow">
  <meta name="robots" content="none">
  <title>پنل مدیریت | زئوس بلاگ</title>
  <link rel="icon" href="/assets/logos/icon.png" sizes="32x32">
  <link rel="stylesheet" href="/assets/fonts/vazir.css">
  <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v7.1.0/css/all.css">
<style>
    :root {
      --primary: #4361ee;
      --primary-dark: #3a56d4;
      --danger: #e74c3c;
      --success: #28a745;
      --gray: #f8f9fa;
      --light: #f8f9ff;
      --dark: #212529;
    }
    * { 
        margin: 0; 
        padding: 0; 
        box-sizing: border-box; 
    }
    
    body {
      font-family: 'vazir', Tahoma, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 15px;
      color: var(--dark);
      line-height: 1.6;
    }
    
    .container { 
        max-width: 1100px; 
        margin: 20px auto;
        width: 100%;
    }
    
    .user-info {
      text-align: center;
      margin: 15px 0;
      padding: 15px;
      background: rgba(255,255,255,0.95);
      border-radius: 15px;
      font-size: 16px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    }
    
    .user-info a { 
        color: var(--danger); 
        font-weight: bold; 
        text-decoration: none; 
    }
    
    .user-info a:hover { 
        text-decoration: underline; 
    }

    .error-msg {
        background: #ffebee;
        color: #c62828;
        padding: 12px 15px;
        border-radius: 10px;
        border-right: 4px solid #e74c3c;
        margin: 15px 0;
        text-align: center;
        font-weight: bold;
        animation: slideDown 0.6s ease-out;
        font-size: 14px;
    }
    
    .header {
      background: white;
      padding: 25px 30px;
      border-radius: 20px 20px 0 0;
      text-align: center;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }
    
    .header h1 {
      color: var(--primary);
      font-size: 28px;
      margin: 0;
      font-weight: 700;
      line-height: 1.3;
    }

    .card {
      background: white;
      border-radius: 0 0 20px 20px;
      padding: 30px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      margin-top: -8px;
    }

    .actions {
      text-align: center;
      margin: 25px 0;
    }
    
    .btn {
      display: inline-block;
      background: var(--primary);
      color: white;
      padding: 12px 28px;
      border: none;
      border-radius: 12px;
      font-size: 15px;
      font-weight: bold;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 8px 25px rgba(67,97,238,0.3);
    }
    
    .btn:hover {
      background: var(--primary-dark);
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(67,97,238,0.4);
    }

    .success-msg {
      padding: 18px;
      border-radius: 15px;
      text-align: center;
      font-weight: bold;
      font-size: 16px;
      margin: 20px 0;
      border-right: 5px solid;
      animation: slideDown 0.6s ease-out;
    }
    
    .success-msg.create { 
        background: linear-gradient(90deg, #d4edda, #c3e6cb); 
        color: #155724; 
        border-color: var(--success); 
    }
    
    .success-msg.edit   { 
        background: linear-gradient(90deg, #d1ecf1, #b8daff); 
        color: #0c5460; 
        border-color: #17a2b8; 
    }
    
    .success-msg.delete { 
        background: linear-gradient(90deg, #f8d7da, #f5c6cb); 
        color: #721c24; 
        border-color: var(--danger); 
    }
    
    @keyframes slideDown { 
        from { 
            opacity: 0; 
            transform: translateY(-20px); 
        } 
        to { 
            opacity: 1; 
            transform: translateY(0); 
        } 
    }

    .post-list { 
        display: grid; 
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 18px; 
        margin-top: 20px; 
    }
    
    .post-item {
      background: var(--light);
      padding: 15px 20px;
      border-radius: 16px;
      border-right: 5px solid var(--primary);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .post-item:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 30px rgba(0,0,0,0.12);
    }
    
    .post-title {
      font-size: 20px;
      font-weight: bold;
      color: var(--dark);
      margin-bottom: 12px;
      line-height: 1.4;
    }
    
    .post-meta {
      font-size: 14px;
      color: #555;
      margin: 12px 0;
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: center;
    }
    
    .post-id {
      background: var(--primary);
      color: white;
      padding: 5px 12px;
      border-radius: 10px;
      font-family: 'Courier New', monospace;
      font-weight: bold;
      font-size: 12px;
    }
    
    .post-actions {
      margin-top: 15px;
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      justify-content: flex-start;
    }
    
    .edit-link, .delete-btn {
      padding: 8px 16px;
      border-radius: 10px;
      font-weight: bold;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s ease;
      font-size: 14px;
      border: 2px solid transparent;
      flex: 1;
      min-width: 120px;
      text-align: center;
    }
    
    .edit-link {
      background: #e3f2fd;
      color: #1565c0;
      border-color: #bbdefb;
    }
    
    .edit-link:hover {
      background: #bbdefb;
      transform: translateY(-2px);
    }
    
    .delete-btn {
      background: transparent;
      color: var(--danger);
      border-color: var(--danger);
      font-family: 'vazir';
    }
    
    .delete-btn:hover {
      background: var(--danger);
      color: white;
      transform: translateY(-2px);
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      background: var(--light);
      border-radius: 20px;
      color: #777;
      font-size: 18px;
      grid-column: 1 / -1;
    }
    
    .empty-state i {
      font-size: 70px;
      color: #ccc;
      margin-bottom: 20px;
    }

    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0; 
      top: 0;
      width: 100%; 
      height: 100%;
      background: rgba(0,0,0,0.65);
      backdrop-filter: blur(6px);
      align-items: center;
      justify-content: center;
      padding: 15px;
    }
    
    .modal-content {
      background: white;
      border-radius: 20px;
      width: 100%;
      max-width: 480px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.25);
      overflow: hidden;
      animation: modalZoom 0.3s ease-out;
    }
    
    @keyframes modalZoom { 
        from { 
            opacity: 0; 
            transform: scale(0.9); 
        } 
        to { 
            opacity: 1; 
            transform: scale(1); 
        } 
    }
    
    .modal-header {
      background: var(--danger);
      color: white;
      padding: 22px 25px;
      text-align: center;
      font-size: 20px;
      font-weight: bold;
    }
    
    .modal-body {
      padding: 25px;
      text-align: center;
      font-size: 16px;
      line-height: 1.5;
    }
    
    .post-title-to-delete {
      background: #fff5f5;
      color: var(--danger);
      padding: 12px;
      border-radius: 12px;
      margin: 20px 0;
      font-weight: bold;
      font-size: 16px;
      border: 2px dashed #ffcdd2;
      word-break: break-word;
    }
    
    .modal-actions {
      display: flex;
      gap: 15px;
      justify-content: center;
      margin-top: 25px;
      flex-wrap: wrap;
    }
    
    .btn-cancel, .btn-danger {
      padding: 12px 25px;
      border: none;
      border-radius: 12px;
      font-weight: bold;
      font-size: 14px;
      cursor: pointer;
      transition: all 0.3s ease;
      min-width: 120px;
      font-family: 'vazir';
    }
    
    .btn-cancel {
      background: #e9ecef;
      color: #495057;
    }
    
    .btn-cancel:hover {
      background: #dee2e6;
      transform: translateY(-2px);
    }
    
    .btn-danger {
      background: var(--danger);
      color: white;
    }
    
    .btn-danger:hover { 
        background: #c0392b; 
        transform: translateY(-2px); 
    }

    @media (max-width: 1024px) {
        .container {
            margin: 15px auto;
            padding: 0 10px;
        }
        
        .post-list {
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 15px;
        }
    }

    @media (max-width: 768px) {
        body {
            padding: 10px;
        }
        
        .container {
            margin: 10px auto;
        }
        
        .header, .card { 
            padding: 20px; 
            border-radius: 16px; 
        }
        
        .header h1 {
            font-size: 24px;
        }
        
        .card {
            padding: 20px;
            margin: 16px auto;
        }
        
        .user-info {
            font-size: 14px;
            padding: 12px;
            margin: 10px 0;
        }
        
        .btn {
            padding: 10px 22px;
            font-size: 14px;
        }
        
        .post-list {
            grid-template-columns: 1fr;
            gap: 12px;
        }
        
        .post-item {
            padding: 12px 16px;
        }
        
        .post-title {
            font-size: 18px;
        }
        
        .post-meta {
            font-size: 13px;
            gap: 8px;
        }
        
        .post-actions {
            flex-direction: column;
            gap: 8px;
        }
        
        .edit-link, .delete-btn {
            min-width: auto;
            width: 100%;
        }
        
        .success-msg {
            font-size: 14px;
            padding: 15px;
            margin: 15px 0;
        }
        
        .empty-state {
            padding: 40px 15px;
            font-size: 16px;
        }
        
        .empty-state i {
            font-size: 50px;
        }
        
        .modal-content {
            max-width: 95%;
            border-radius: 16px;
        }
        
        .modal-header {
            padding: 18px 20px;
            font-size: 18px;
        }
        
        .modal-body {
            padding: 20px;
            font-size: 15px;
        }
        
        .modal-actions {
            flex-direction: column;
            gap: 10px;
        }
        
        .btn-cancel, .btn-danger {
            width: 100%;
            min-width: auto;
        }
    }

    @media (max-width: 480px) {
        .header h1 {
            font-size: 20px;
        }
        
        .post-title {
            font-size: 16px;
        }
        
        .post-meta {
            flex-direction: column;
            align-items: flex-start;
            gap: 6px;
        }
        
        .modal-body {
            padding: 15px;
        }
        
        .post-title-to-delete {
            font-size: 14px;
            padding: 10px;
        }
    }

    @media (max-height: 500px) and (orientation: landscape) {
        .modal {
            align-items: flex-start;
            padding-top: 20px;
        }
        
        .modal-content {
            max-height: 90vh;
            overflow-y: auto;
        }
    }
</style>
</head>
<body>

  <div class="container">
    <div class="user-info">
      خوش آمدی، <strong><%= session.username || 'ادمین' %></strong> 
      | <a href="<%= basePath %>/logout">خروج</a>
    </div>

    <div class="header">
      <h1>پنل مدیریت زئوس بلاگ</h1>
    </div>

    <div class="card">
      <% if (success === '1') { %>
        <div class="success-msg create">پست با موفقیت منتشر شد! آماده فتح وب شدید!</div>
      <% } else if (success === 'edited') { %>
        <div class="success-msg edit">تغییرات با موفقیت ذخیره شد! آیدی جدید اعمال شد</div>
      <% } else if (success === 'deleted') { %>
        <div class="success-msg delete">پست با موفقیت حذف شد!</div>
      <% } %>

      <div class="actions">
        <a href="<%= basePath %>/new" class="btn">نوشتن پست جدید</a>
      </div>

      <div class="post-list">
        <% if (posts.length === 0) { %>
          <div class="empty-state">
            <i class="fas fa-feather-alt"></i><br><br>
            هنوز پستی منتشر نشده است<br>
            <strong>اولین شاهکارت را همین الان بساز!</strong>
          </div>
        <% } else { %>
          <% posts.forEach(post => { %>
            <div class="post-item">
              <div class="post-title"><%= post.title %></div>
              
              <div class="post-meta">
                <span><%= post.date %></span>
                <span><%= post.category || 'بدون دسته' %></span>
                <span class="post-id"><%= post.id %></span>
              </div>

              <div class="post-actions">
                <a href="<%= basePath %>/edit/<%= post.id %>" class="edit-link">ویرایش پست</a>
                <button onclick="openDeleteModal('<%= post.id %>', '<%= post.title %>')" class="delete-btn">
                  حذف پست
                </button>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </div>

  <div id="deleteModal" class="modal">
    <div class="modal-content">
      <div class="modal-header">حذف پست</div>
      <div class="modal-body">
        <p>آیا مطمئنی می‌خوای این پست رو <strong>برای همیشه</strong> حذف کنی؟</p>
        <div class="post-title-to-delete"></div>
        <div class="modal-actions">
          <button onclick="closeDeleteModal()" class="btn-cancel">خیر، لغو کن</button>
          <button onclick="confirmDelete()" class="btn-danger">بله، حذفش کن</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    let postIdToDelete = '';

    function openDeleteModal(id, title) {
      postIdToDelete = id;
      document.querySelector('.post-title-to-delete').textContent = title;
      document.getElementById('deleteModal').style.display = 'flex';
    }

    function closeDeleteModal() {
      document.getElementById('deleteModal').style.display = 'none';
      postIdToDelete = '';
    }

    function confirmDelete() {
      if (postIdToDelete) {
        window.location.href = '<%= basePath %>/delete/' + postIdToDelete;
      }
    }

    window.onclick = function(e) {
      const modal = document.getElementById('deleteModal');
      if (e.target === modal) closeDeleteModal();
    }
  </script>
</body>
</html>